chargegrid {
    db {
        url = "jdbc:mysql://localhost:3306/chargegrid"
        driver = "com.mysql.cj.jdbc.Driver"
        user = "root"
        password = "302006"
        maxPoolSize = 10
    }
    jwt {
        secret = "change-this-to-a-very-long-secret-key-in-production"
        issuer = "chargegrid"
        audience = "chargegrid-users"
        expiresInHours = 24
    }
    server {
        port = 8081
    }
}
